# Hook logs from the auto-deck pipeline
.claude/skills/codebase-to-slides/logs/

# Cached Google Drive folder id (machine-specific)
.claude/skills/codebase-to-slides/.drive_folder_id

# Personal Claude Code settings (bypass mode is per-user, not shared)
.claude/settings.local.json

# Stray local venvs (no longer used)
.venv-slides/

# Knowledge-sync hook log (local only; entries/ IS tracked and shared)
.claude/hooks/knowledge-sync.log
